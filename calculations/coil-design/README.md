# Coil Design Calculations

Placeholder for the numerical models that will quantify \(P_{\rm field}\).

Planned files:

- `toroidal_field.py` — idealized NI and B estimates
- `coil_sweep.py` — parameter sweep over N, I, conductor size, temperature
- `results.csv` — output table

**Do not treat any numbers as final until conductor geometry, thermal boundaries, and excitation mode are specified.**
